import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  sun: {
    width: 170,
    height: 100,
    borderRadius: 100,
    backgroundColor: '#eaeaea',
    marginLeft: 150
  }
})
