import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  cloud: {
    width: 220,
    height: 120,
    position: 'absolute',
  },
  cloudsWrapper: {
    position: 'absolute',
    width: '100%',
    height: '100%'
  }
})
